{% extends 'base.html.twig' %}

{% block title %}
	{{ 'user.Edit_user'| trans  }}
{% endblock %}

{% block sous_title %}
<a class="navbar-brand" href="{{ path('projets_index') }}">
	<img src="{{ asset('images/amdie.png') }}" width="30" height="30" class="d-inline-block align-top"
		alt="logo">
		<div class="SousTitre">
		<strong>AMDIE</strong>
	|
	{{ 'user.titre_user'| trans | upper }}
	</div>
</a>
{% endblock %}
{% block navbar_right %}{% endblock %}
{% block navbar_left %}
	{# <a class="btn btn-blue" href="{{ path('user_index') }}">{{'user.Back_to_list'| trans}}</a> #}
	<h4 class="navbar-text-color">{{ 'user.Edit_user'| trans }}</h4>
{% endblock %}
{% block body %}

	<div class="card">
		<div class="card-body">
			{{ include('user/_form.html.twig', {'button_label': 'user.Update'| trans}) }}
		</div>
	</div>
<script>
$(document).ready(function() {
    $( "#edit_user_groupe" ).prop( "disabled", true );
	$( "#edit_user_email" ).prop( "disabled", true );
	$( "#edit_user_secteurs" ).prop( "disabled", true );
	$( "#edit_user_photo" ).prop( "disabled", true );
});
</script>
{% endblock %}

