<form method="post" action="{{ path('acces_groupe_delete', {'id': acces_groupe.id}) }}" class="form_delete">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acces_groupe.id) }}">
    <button class="btn">Delete</button>
</form>
