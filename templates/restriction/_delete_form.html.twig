<form method="post" id="delete-form" action="{{ path('restriction_delete', {'id': restriction.id}) }}">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restriction.id) }}">
	<button class="btn btn-blue" id="delete-item">{{ 'restriction.Delete'| trans }}</button>
</form>
