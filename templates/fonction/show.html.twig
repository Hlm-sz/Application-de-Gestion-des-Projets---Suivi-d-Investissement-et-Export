{% extends 'base.html.twig' %}

{% block title %}
	{{'canal.Detail'| trans}}
{% endblock %}
{% block sous_title %}
<a class="navbar-brand" href="{{ path('fonction_index') }}">
	<img src="{{ asset('images/amdie.png') }}" width="30" height="30" class="d-inline-block align-top"
		alt="logo">
		<div class="SousTitre">
		<strong>AMDIE</strong>
	|
	{{"Détails de fonction" }}
	</div>
</a>
{% endblock %}
{% block navbar_right %}
	<a class="btn btn-blue" href="{{ path('fonction_edit', {'id': fonction.id}) }}">{{"Modifier"}}</a>
	{{ include('fonction/_delete_form.html.twig') }}
{% endblock %}
{% block navbar_left %}
	<a class="btn btn-blue" href="{{ path('fonction_index') }}">
		<i class="fa fa-arrow-left"></i>
	</a>
	<h4 class="navbar-text-color">{{"Détails de fonction"}}</h4>
{% endblock %}

{% block body %}

	<div class="card">
		<div class="card-body">
			<table class="table">
				<tbody>
					<tr>
						<th>{{"Fonction" }}</th>
						<td>{{ fonction.nom }}</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
{% endblock %}
