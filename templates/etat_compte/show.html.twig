{% extends 'base.html.twig' %}

{% block title %}EtatCompte{% endblock %}

{% block body %}
    <h1>EtatCompte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_compte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etat_compte.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etat_compte_index') }}">back to list</a>

    <a href="{{ path('etat_compte_edit', {'id': etat_compte.id}) }}">edit</a>

    {{ include('etat_compte/_delete_form.html.twig') }}
{% endblock %}
