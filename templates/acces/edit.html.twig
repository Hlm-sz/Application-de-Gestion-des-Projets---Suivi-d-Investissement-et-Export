{% extends 'base.html.twig' %}

{% block title %}
	{{ 'acces.Edit_acces'| trans }}
{% endblock %}
{% block sous_title %}
	{{ 'acces.Acces'| trans }}
{% endblock %}
{% block body %}

	<div class="card">
		<div class="card-header">
			<a class="btn btn-primary" href="{{ path('acces_index') }}">{{ 'acces.Back_to_list'| trans }}</a>
			{{ 'acces.Edit_acces'| trans }}
		</div>
		<div class="card-body">

			{{ include('acces/_form.html.twig', {'button_label': 'acces.Update'| trans }) }}

			{{ include('acces/_delete_form.html.twig') }}

		</div>
	</div>
{% endblock %}
