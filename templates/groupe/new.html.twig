{% extends 'base.html.twig' %}

{% block title %}
	{{ 'groupe.New_groupe'| trans }}
{% endblock %}
{% block sous_title %}
<a class="navbar-brand" href="{{ path('groupe_index') }}">
	<img src="{{ asset('images/amdie.png') }}" width="30" height="30" class="d-inline-block align-top"
		alt="logo">
		<div class="SousTitre">
		<strong>AMDIE</strong>
	|
	{{ 'groupe.titre_groupe'| trans | upper }}
	</div>
</a>
{% endblock %}
{% block navbar_right %}{% endblock %}
{% block navbar_left %}
	<a class="btn btn-blue" href="{{ path('groupe_index') }}">{{'groupe.Back_to_list'| trans}}</a>
	<h4 class="navbar-text-color">{{'groupe.New_groupe'| trans}}</h4>
{% endblock %}
{% block body %}


	<div class="card">

		<div class="card-body">

			{{ include('groupe/_form.html.twig') }}

		</div>
	</div>
{% endblock %}
