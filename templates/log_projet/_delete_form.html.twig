<form method="post" action="{{ path('log_projet_delete', {'id': log_projet.id}) }}"  class="form_delete">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log_projet.id) }}">
    <button class="btn">Delete</button>
</form>
